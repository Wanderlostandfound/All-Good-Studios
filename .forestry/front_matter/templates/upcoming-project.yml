---
label: Upcoming Project
hide_body: false
is_partial: false
fields:
- type: text
  name: title
  label: Title
  description: Name of the Project
  config:
    required: true
- type: text
  name: description
  label: Description
  description: One sentence that is displayed over the featured image on the page
    preview tiles
  config:
    required: true
- type: file
  label: Featured Img
  name: featured-img
  description: Choose a vertical image for best results
  config:
    maxSize: '3'
- type: file
  label: Header Img
  name: header-img
  description: choose a horizontal image for best results
  config:
    maxSize: '3'
- type: select
  name: sector
  config:
    source:
      type: simple
    required: true
    options:
    - Hospitality
    - Commercial
    - Residential
    - Retail
  label: Sector
  description: Pick a sector from the list
  default: Hospitality
- type: text
  label: Creating
  name: created
  description: List of values that comes after the text "We are creating:"
  config:
    required: true
- type: file
  name: horizontal-img1
  label: Horizontal img1
  description: choose a horizontal image for best results. 3MB maximum
  config:
    maxSize: '3'
- type: text
  name: layout
  label: layout
  config:
    required: true
pages:
- _portfolio/testing.md
